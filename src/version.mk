# Version numbers are set here and here only.  They get passed to the
# compiler via the -D option and are also imported into bash to inject
# the values into the environment (so this ALSO needs to be valid bash
# syntax.)
#
# Note that PATCHLEVEL changes should not break savefile
# compatibility.
RELARN_VERSION=2.2
RELARN_PATCHLEVEL=1
