# Version numbers are set here and here only.  They get passed to the
# compiler via the -D option.
#
# Note that PATCHLEVEL changes should not break savefile
# compatibility.

RELARN_VERSION = 2.1
RELARN_PATCHLEVEL = 1
